#include "tracker.h"
#include <iostream>
#include <fstream>  /* For file operations
assuming that we will be using save files to store our subjects so you can use the program more than once, lol */


//here we will put the actual function definitions for main.cpp to use (protypes in tracker.h)
void loadSubjects() {
    std::cout << "Loading subjects..." << std::endl;
}

void saveSubjects() {
    std::cout << "Saving subjects..." << std::endl;
}

void addSubject() {
    std::cout << "Adding a subject..." << std::endl;
}
